apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp
  namespace: webapp
  labels:
    app: webapp    
spec:
  rules:
  - host: webapp.vigne.com.br
    http:
      paths:
      - backend:
          service:
            name: webapp
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
      - webapp.vigne.com.br
